<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.appz.qrcode">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/main_ic2"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".seller_tasks.SellActivity"></activity>
        <activity android:name=".seller_tasks.EditedStoreActivity" />
        <activity android:name=".client_tasks.ProfileActivity" />
        <activity android:name=".client_tasks.GenerateQrCodeActivity" />
        <activity android:name=".seller_tasks.SellerActivity" />
        <activity android:name=".client_tasks.ClientActivity" />
        <activity android:name=".login_tasks.RegisterionActivity" />
        <activity android:name=".login_tasks.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".detect_text"
            android:parentActivityName=".login_tasks.LoginActivity" />
        <activity
            android:name=".MainActivity"
            android:parentActivityName=".detect_text" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/AppTheme" />
    </application>

</manifest>